#프로그램 요소

> fun
- 함수를 의미

> fun main() {}
- ()안에는 인수 또는 args(사용할 정보)를 넣을 수 있음
- {}안에는 작업을 실행하는 코드를 넣을 수 있음
- ""안에는 출력될 텍스트를 넣을 수 있음

예) println("Happy Birthday!")
println - 출력함수
() - 출력할 텍스트 입력
"Happy Birthday!" - 텍스트
출력결과 >> Happy Birthday!

*텍스트를 실제로 출력하려면 전체 println명령이 main함수 내에 있어야 함
